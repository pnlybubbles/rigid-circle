{
  "name": "raw-rigid-circle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "./node_modules/.bin/watchify -v -d index.babel.js -t [ babelify --presets [ es2015 ] ] -o 'exorcist index.js.map > index.js'"
  },
  "author": "pnlybubbles",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "exorcist": "^0.4.0",
    "watchify": "^3.7.0"
  }
}
